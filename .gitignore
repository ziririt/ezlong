.next/
node_modules/
**/node_modules/
**/.gradle/
**/build/
**/Pods/
.env
.env.local
.DS_Store

# 세션 전 로컬 백업 (git 추적 제외)
.backup/

# 사진 이미지 아카이브 (Firebase Storage 별도 관리 — git 추적 절대 금지)
# 원본 고해상도 사진은 이 폴더들에 두고 scripts/upload-image-archive.js로 업로드한다.
# git에는 이미지 실물이 아니라 data/image-archive-manifest.json(URL 목록)만 들어간다.
_image-archive-staging/
firebase-service-account*.json
*serviceAccountKey*.json

# 2026-07-30 (44-1 이사 정리): 캐시·임시·백업 — git 추적 금지
.firebase/
__pycache__/
_to_delete/
*.bak
*.bak-*
*.bak_*
*.BACKUP-*
*.html.new
.synctest.txt
scripts/validate-photo-classifier-result*.json
