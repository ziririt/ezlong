# 승계: 내가 못 하게 되면 읽을 문서

성동님(김성동)이 갑자기 일을 볼 수 없게 됐을 때, **다른 사람이 ezlong.com을 이어서 운영·개발할 수 있게** 쓴다.
기술은 문서로 넘길 수 있다. **계정은 문서로 못 넘긴다.** 그래서 이 문서의 절반은 성동님이 미리 해둘 일이다.

최종 갱신: 2026-09-06

---

## 1. 이어받는 사람에게

당신이 개발자가 아니어도 된다. 이 프로젝트는 원래 사람이 화면을 보고 지적하고 AI가 코드를 쓰는 방식으로 굴러왔다.

1. 이 저장소의 `HANDOFF.md`를 읽는다. 지금 상태 한 장이다.
2. `docs/RECOVERY.md`대로 맥을 준비한다. 30분.
3. 클로드(Cowork)를 열고 `HANDOFF.md` 맨 끝의 문장을 붙여넣는다. AI가 `CLAUDE.md`(규칙 86항)를 읽고 상황을 보고한다.
4. 처음 며칠은 **고치지 말고 지켜본다.** 사이트는 봇이 자동으로 돌린다. 급한 것은 없다.
   급한 것이 생기면 `docs/BACKUP.md` 4장(원복)으로 되돌리면 된다.
5. 매일 09:00 KST 클라우드 예약 작업이 파이프라인·키 만료를 점검해 알린다. 그 알림은 성동님 계정으로 간다.
   이어받았으면 그 작업을 당신 계정에 다시 만든다(내용은 `docs/api-keys.md` 끝 참조).

## 2. 필요한 계정과 권한 (이것이 전부다)

- **GitHub `ziririt/ezlong`**: 코드·데이터·문서·시크릿·자동화 전부. **가장 중요하다.**
  협업자(Collaborator, Admin) 권한이 있어야 시크릿과 워크플로를 만질 수 있다.
- **Firebase 프로젝트**(Hosting): 라이브 배포와 롤백. 프로젝트 소유자(Owner) 권한.
- **Google 계정 `ziririt@gmail.com`**: Search Console(속성 ezlong.com), AdSense(pub-2336764115275414), AI Studio(Gemini 키).
- **Anthropic 콘솔**: Claude API 키(스코어카드·스윙).
- **도메인 등록기관**: ezlong.com 갱신. 만료되면 사이트가 사라진다. **가장 놓치기 쉽다.**
- **기타 API**: FRED, Alpha Vantage 등. 무료 재발급 가능. `docs/api-keys.md`.
- **클로드(Cowork) 계정**: 없어도 된다. 이어받는 사람 것으로 새로 시작하면 된다.

키 값은 넘겨받을 필요가 없다. **계정에 들어가면 전부 재발급된다.** 그래서 계정 접근이 전부다.

## 3. 성동님이 미리 해둘 일 (이 문서가 살려면 이것이 필요하다)

체크하고 날짜를 적는다. 비어 있으면 승계는 문서만 있고 열쇠가 없는 상태다.

- [ ] **GitHub 저장소에 두 번째 관리자 추가**: Settings → Collaborators → 신뢰하는 사람 1명, Admin.
      또는 GitHub Organization으로 옮기고 소유자를 둘 둔다. (날짜: )
- [ ] **Firebase 프로젝트에 두 번째 소유자 추가**: 콘솔 → 프로젝트 설정 → 사용자 및 권한. (날짜: )
- [ ] **Google 계정 휴면 계정 관리자 설정**: myaccount.google.com → 데이터 및 개인 정보 보호 → 휴면 계정 관리자.
      일정 기간 접속이 없으면 지정한 사람에게 접근을 넘긴다. Search Console·AdSense·Gemini가 여기 달려 있다. (날짜: )
- [ ] **도메인 등록기관 자동 갱신 켜기 + 결제수단 유효기간 확인 + 비상 연락처 추가.** (날짜: )
- [ ] **봉인 서한 한 장** (종이 또는 비밀번호 관리자의 긴급 접근):
      "ezlong.com은 github.com/ziririt/ezlong 에 있다. docs/SUCCESSION.md 를 읽어라."
      + 각 계정의 로그인 방법(비밀번호 관리자 이름과 긴급 접근 방법). **키 값이 아니라 문 여는 법.** (날짜: )
- [ ] 이어받을 사람에게 이 문서의 존재를 **말로** 알린다. 문서는 아는 사람에게만 문서다. (날짜: )

## 4. 이어받는 사람이 첫 주에 할 일

- 1일: RECOVERY.md 완료. 라이브 확인. 아무것도 고치지 않는다.
- 2일: 모든 API 키를 **본인 명의로 재발급**해 시크릿 교체. 옛 키 폐기. `docs/api-keys.md` 갱신.
- 3일: `docs/BACKUP.md` 3장 외부 미러를 본인 계정으로 다시 잇는다.
- 7일: HANDOFF.md를 본인 이름으로 다시 쓴다. 인수인계는 통째로 덮어쓰는 문서다.

## 5. 왜 이렇게까지 하나

이 사이트를 읽는 사람들은 하루 8번 갱신되는 판정을 믿고 있다. 운영자가 사라졌을 때
사이트가 조용히 낡아 가는 것이 최악이다. 봇은 계속 돌겠지만 키가 만료되는 순간(3개월) 멈춘다.
**3개월. 그 안에 누군가 이 문서를 읽고 계정에 들어가면 된다.** 그래서 2장과 3장이 이 문서의 전부다.
