# EZLONG.COM — Claude 자동 로드 지침 (상시본)

> **여러 프로젝트가 함께 지키는 운용 규칙은 별도의 비공개 운용 저장소(`_ops`)에 있다.
> 세션을 시작하면 그곳의 `RULES.md` · `BOARD.md` · `projects.md` 를 먼저 읽는다.**
> 기기 한 대를 여러 세션이 나눠 쓰기 때문에 부딪히는 자원과 순서가 거기 적혀 있다.
> **충돌하면 이 문서가 이긴다** — 공용 규칙은 바닥이지 천장이 아니다. (2026-09-12 신설)

> 이 파일은 매 세션 시작 시 자동으로 로드된다.
> 전체 개발 가이드는 **EZLONG_GUIDE.md** 를 반드시 먼저 읽을 것.

---

## 0. 이 문서를 읽는 법 (2026-09-12 재편, 성동님 확정)

규칙은 세 파일로 나뉘어 있다. **한 줄도 지우지 않았다. 읽는 시점으로 갈랐을 뿐이다.**

- **`CLAUDE.md`(이 파일)** — 상시본. 어느 파일을 만지든 적용되는 것. **전문을 읽는다.**
- **`docs/RULES-REF.md`** — 기능별 참조 42항. 그 코너·파이프라인을 실제로 만질 때 연다.
- **`docs/INCIDENTS.md`** — 사고 기록 52항. 규칙을 되돌리고 싶어질 때, "왜 이렇게 하나"가
  궁금할 때 연다. 상시 규칙의 원문 서술도 전부 여기 있다.

**찾는 것이 이 파일에 없으면 맨 아래 색인을 본다.** 94개 항이 전부 어느 파일에 있는지 적혀 있다
(번호가 없는 앞머리 6항은 제목으로 찾는다). **항 번호는 절대 바꾸지 않는다** — 코드 주석과 다른
문서가 "52항 경로를 탄다" 식으로 번호로 서로를 가리킨다.

왜 갈랐나: 한 항이 **매번 읽어야 할 원칙**과 **한 번만 읽으면 되는 사고 기록**을 같이
들고 있었다. 85항은 69줄인데 원칙은 3줄이고 나머지 66줄이 기록이다. 원칙 3줄을 찾으려고
66줄을 지나가야 했고, 94개 항이 전부 그 모양이었다.

---

## 1. 배포 — push 가 곧 배포다

### 세션 시작 전 백업 먼저 (필수)

2026-06-14 에 HTML·이미지·알고리즘이 통째로 유실돼 14시간을 재구축했다. 원인은 로컬
미커밋 파일이 `git pull --rebase` 로 5월 27일 버전에 덮인 것이다. **파일 수정을 시작하기
전에** 성동님께 아래 실행을 요청한다.

```bash
sh ~/Developer/ezlong/backup-before-session.sh
```

**백업 완료 메시지를 확인한 뒤에만 파일 수정을 시작한다.**

맥의 `com.ezlong.daily-backup` LaunchAgent 가 매일 12:30 에 같은 것을 돌린다.
GitHub 쪽 되감기 지점(일·주·월 태그, 번들)과 새 맥 복구·승계 절차는 87-2항과
`docs/BACKUP.md` · `docs/RECOVERY.md` · `docs/SUCCESSION.md`.


`git pull` → `git add [파일 명시]` → `git commit` → `git push`. **push 하면 GitHub
Actions 가 Firebase 로 자동 배포한다.** 수동 `firebase deploy` 는 Actions 장애 시
비상용으로만 — 평소에 쓰면 로컬 잡파일이 함께 올라가고 다음 자동 배포가 조용히 되돌린다.

**클라우드 세션(지금 기본 무대)에서는 샌드박스에서 직접 커밋한다.** 2항의 "sandbox
git 쓰기 금지"는 iCloud 동기화 폴더의 `index.lock` 충돌이 근거였고(26항), 클라우드
샌드박스는 iCloud 와 무관한 깨끗한 클론이라 적용 대상이 아니다(28항, 성동님 확정).
다만 **샌드박스에서 push 는 막혀 있다(프록시 403)** — `git format-patch` 로 패치를
만들어 맥에 옮기고 맥에서 `git am` + `push` 한다. 절차는 `HANDOFF.md` 7-2절.

### 가드레일 (협상 불가)

- `git add -A` 금지 — 파일을 명시한다
- `git reset --hard` 금지 — tracked 파일이 소멸한다(16항에서 실제로 났다)
- force push 금지, 히스토리 재작성 금지
- push 전 diff 확인. 10개 이상 파일·핵심 알고리즘 변경은 계획을 먼저 세운다
- 어떤 열쇠도 커밋하지 않는다(87항 deny 규칙과 같은 목록) — `.env`,
  `serviceAccountKey*.json`, `*-firebase-adminsdk-*.json`, `*credentials*.json`,
  `.firebase/**`, `*.pem`, `*.p12`. 앱 스토어 열쇠(`~/.appstoreconnect` 의 `.p8`·`.jks`)도
  저장소로 복사하지 않는다.
  **`deny` 는 실수를 줄이는 장치이지 경계가 아니다** — Bash 는 덮지 못한다.
  진짜 경계는 `.gitignore` 와 배포 전 게이트, 그리고 파일 권한이다
- 다른 세션이 남긴 미커밋 변경과 미push 커밋을 지우지 않는다. 앞 주자가 일하는 중일 수 있다
- 미커밋 변경을 남긴 채 세션을 끝내지 않는다. 완성 전이면 `wip/<슬러그>` 브랜치로라도 올린다
- **push 결과를 매번 채팅으로 보고한다**
- **배포마다 앱 저장소 `RELEASES.md` 에 한 줄 기록 + deploy 태그**(44-2 버전표·롤백 체계)
- 커밋 메시지 끝 두 줄:
  `Co-Authored-By: <그 세션의 모델명> <noreply@anthropic.com>` / `Claude-Session: <세션 URL>`
  (세션 URL 이 없는 주자는 `Claude-Session: supergrok` 처럼 출처를 적는다. 빈칸으로 두지 않는다)

### 이미지는 단독 커밋

새 이미지는 코드 수정과 분리해 먼저 단독으로 push 한다. 안 그러면 Firebase 에서 엑박이 뜬다.

### 배포는 라이브 확인까지가 한 덩이다 (7항)

push 했다고 끝이 아니다. 반영이 의심되면 **가장 먼저** `ezlong.com/ez-nav.js` 와
`ezlong-541a8.web.app/ez-nav.js` 를 **둘 다** 받아 버전을 비교한다.

- web.app 이 구버전 → Actions 실패. 로그 확인 후 재실행
- web.app 은 신버전, ezlong.com 이 구버전 → Cloudflare 캐시 퍼지(성동님 로그인 필요)
- 둘 다 신버전인데 브라우저만 구버전 → **이때만** 강력새로고침 권유

**"캐시 삭제하라"를 첫 대응으로 하지 않는다.** 2026-06-14 오진의 원인이었다.

### 체크포인트와 롤백 (30항)

의미 있는 묶음을 배포 완료한 직후 `cp-YYYYMMDD-슬러그` 태그를 스스로 찍는다.
위험한 작업 **전에는** `stable-YYYYMMDD-슬러그`. 성동님이 "되돌려줘"라고 말씀하시면
**forward-fix 로만** 되돌린다 — `git checkout <태그> -- <파일들>` 후 새 커밋으로 push.
히스토리는 보존한다. `data/` 봇 생성 파일은 롤백 대상에서 제외한다.

---

## 2. 확인 — 선 실행, 후 확인 (45·94항)

**판단이 서면 먼저 한다.** 물어놓고 기다리지 않는다. 안전은 질문이 아니라 체크포인트와
셀프 검증으로 확보한다. 성동님은 26년차 웹기획자이고 개발자가 아니다 — **개발 판단을
성동님께 넘기지 않는다**(48항). 여쭐 것은 결과가 마음에 드는지, 쓰기 불편하지 않은지다.

**예외 둘.** ① 되돌릴 수 없는 것(force push, 히스토리 재작성, 대량 삭제) ② 혼자 정할 수
없는 것(공유 브랜드 자산, 성동님 계정·결제, 법·세무 수치). 그 경우에도 **시안까지는 만든다.**

### 확인 요청의 형식

본문과 확연히 구분되게, 답변 **맨 끝**에, 구분선으로 막고, 번호 택일로 낸다.
성동님이 숫자 하나만 입력하면 끝나야 한다. 한 답변에 확인 묶음은 **하나만**.

```
---

> ### 확인 부탁드립니다 — <무엇을 정하는지>
>
> <이렇게 해두었습니다. 한두 줄.>

    1  좋다 - 이대로 간다
    2  <다른 안>
    기타 의견은 직접 입력해 주십시오

---
```

### 답을 못 받으면 내가 챙긴다

**`docs/PENDING.md` 에 적는다.** 채팅 스크롤은 기록이 아니다. **세션을 시작하면 이 파일을
먼저 읽고, 열린 항목이 있으면 그 세션 첫 답변에서 다시 올린다.** 다시 올릴 때마다
'다시 올린 날'을 갱신하고, 세 번 넘게 답이 없으면 본문 맨 앞으로 올린다.
"확인했다 / 좋다 / 넘어가자"를 들은 것만 지운다.
**`HANDOFF.md` 에 두지 않는다** — 그 문서는 통째로 덮어쓰므로 열린 확인이 쓸려 사라진다.

---

## 3. 인계 — 주자가 셋이다 (97항)

한도 소진 순서는 1번 `ziririt@gmail.com` → 2번 `ziririt@aladin.co.kr` → 3번 supergrok.
**한도는 예고 없이 찬다. "끝나고 쓰겠다"는 못 쓴다는 뜻이다.**

- **`HANDOFF.md` 는 최신 상태 한 장이다.** 이력을 쌓지 않고 통째로 덮어쓴다.
  이 문서 하나만 읽고 바로 이어서 개발할 수 있어야 한다.
- **갱신 시점:** 배포 완료 / 새 항 번호를 땄을 때 / 파이프라인·공용 자산을 건드렸을 때 /
  커밋이 다섯을 넘었을 때 / `docs/PENDING.md` 에 항목이 생기거나 풀렸을 때.
  **끝에 몰아 쓰지 않는다.**
- **미완이어도 커밋한다.** `wip/<번호>-<슬러그>` 브랜치로 push 하고 무엇이 되어 있고
  무엇이 남았는지 메시지에 적는다. 저장소 밖(tar·바탕화면)에 작업물을 두지 않는다.
- **세션을 시작하면 `git log --oneline -5` 와 `git status` 를 먼저 본다.** 미push 커밋이나
  미커밋 변경이 있으면 앞 주자가 남긴 것이다.

---

## 4. 작업 방식 — 위험 등급제 (18항)

작업이 들어오면 등급부터 매긴다. **잘 돌던 작은 작업에 의식을 붙이지 않는다.**

- **0등급(외과수술)** — CSS·문구·한두 줄. 고치고 → 해당 규칙 grep 셀프체크 → 보고. 끝.
- **1등급(기능·다중 파일)** — Task 리스트로 추적 + 끝에 검증 1회.
- **2등급(고위험)** — 알고리즘 변경(`calcBuyScore`/`calcSellScore`, lev3x 임계값,
  물타기 로직), 10개+ 파일 동시 수정, API 키·보안.
  계획 → 성동님 승인 → 실행 → **제3자 감사**. 계획·실행·검증을 한 호흡에 섞지 않는다.

### 2등급 필수 규율

- **검증은 작성자가 아닌 제3자에게.** 변경을 보지 못한 깨끗한 서브에이전트에게 diff 를
  감사시킨다. **감사자에게 의도를 주지 않는다** — diff·대상 파일·수용 기준만 준다.
  왜 그렇게 짰는지를 알면 관대해진다.
- **숫자 계산은 독립 재현으로 대조한다.** 구현 코드를 참고하지 않고 처음부터 다시 계산한다.

### 멈추는 조건

- 같은 파일·같은 증상을 **3회** 고쳤는데도 안 되면 멈춘다. 시도 내역·실패 이유·다음
  가설을 정리해 보고하고 판단을 기다린다.
- 원래 요청 범위를 크게 벗어나 파일만 늘어나면, 끝까지 다 하지 말고 **중간 결과를 먼저
  보고한다.**
- 세금·법령·수치처럼 틀리면 금전 피해가 가는 것은, 현재 기준을 웹 검색으로 재확인하지
  못했으면 추정치임을 명시하고 확정형으로 쓰지 않는다.

### 검사를 만들 때 (52·76-2·85항)

**"이 검사가 집행할 수 있나"를 같이 묻는다.** 못 하면 "그 대신 무엇이 막아주나"를
답해야 한다. 그 답이 "모델의 선의"면 그건 방어가 아니다. **감지하고도 게시하면 사고다.**

### 규칙을 만들 때 (85·90항)

**좁게 잡으면 표현만 갈아입고 통과한다** — 지적의 사례가 아니라 **지적의 원리**를 옮긴다.
**넓게 잡으면 맞는 문장까지 지운다** — 빠진 문장은 아무도 그리워하지 않아 오탐이 미탐보다
오래 산다. 해법은 방향을 뒤집는 것이다: "금지어가 있는가"가 아니라 **"금지된 주장을
하는가"**. 배포 전에 **통과해야 할 참말 세 개**를 먼저 적고 그것들이 살아남는지 본다.

### 고치고 나면 옆자리를 훑는다 (14·82항)

무엇을 고쳤으면 **그 사실을 말하는 자리가 몇 군데인지부터 센다.** 고치기 전에 세는 것이
순서다. 82항에서 앱 링크 행선지를 세 자리 중 두 자리만 고쳐 한 자리가 조용히 옛 주소를
가리킨 채 남았고, 화면에는 아무 이상이 없었다. **검사 범위가 규칙 범위보다 좁으면 그만큼이
사각지대가 된다**(14항 grep 이 공유 파일을 제외해 11px 배지가 149장에 6주 떠 있었다).

---

## 5. 가독성 — 절대 금지 목록

**100회 이상 같은 지적을 받았다.** 코드 작성 후 즉시 셀프 체크한다.

- **폰트** — HTML DOM 텍스트 **14px 미만 절대 금지**(14항). 본문은 **16px 하한**(29항).
  예외는 TradingView·Chart.js 등이 캔버스에 직접 렌더하는 텍스트뿐.
  소수점(12.5px)도 위반이다. 공유 파일(`ez-nav.js`·`ez-footer.js`·`ez-design.css`)도 검사 대상이다.
- **`var(--ez-hint)` 를 DOM 텍스트 색으로 쓰지 않는다** — 라이트모드 대비 1.6:1.
  `var(--ez-text2)` 를 쓴다.
- **노랑·앰버를 라이트모드 텍스트로 쓰지 않는다** — `IS_DARK ? '#FFD60A' : '#B87900'` 으로 분기.
- **`background:${color}20; color:${color}` 금지** — 배지는 solid 배경 + 흰 글자.
- **회색 배경 위 `--ez-text3` 금지**, `font-weight:300` 금지(최소 400).
- **`--ez-mono` 를 가격·수치에 쓰지 않는다**(15항). 코드 스니펫 전용이다.
  `font-variant-numeric: tabular-nums` 도 같은 이유로 금지.
- **자간은 크기에 따라 다르다**(33항) — 큰 표제는 음수, 작은 본문은 0~약간 양수.
  고정 트래킹은 전 구간 중 한쪽이 반드시 틀린다. `clamp()` 로 크기와 연동한다.

**UI 를 고치면 눈으로 확정한다.** grep 셀프체크 후 브라우저로 실제 렌더해 **라이트·다크
두 모드 × 폰 폭부터** 확인한다. 텍스트 검사로는 밀림·겹침·체감 대비가 안 잡힌다.
**렌더 검사에서 가시성은 조상까지 본다** — 요소 자신의 `display` 만 보면 닫힌 메뉴 안의
요소가 위반으로 잡혀 오탐이 난다.

**밀도·가독성은 가장 좁은 화면을 기준으로 잡고 넓은 쪽에서 완화한다**(44항).

---

## 6. 디자인 — 방향과 기준 (31·33·50항)

세 축: **Apple.com 의 여백과 타이포그래피**, **iOS·visionOS 의 반투명 글라스**,
**테슬라의 미니멀리즘과 제1원리**. 판단이 갈릴 때의 질문 — "머스크라면 이 요소를 지웠을까."
**추가보다 삭제.** 버튼·설명·옵션이 하나 줄어들 수 있는지 먼저 묻는다.

- **가독성 절대 규칙이 항상 우선한다.** 글라스 때문에 대비비나 최소 폰트를 깨지 않는다.
  `backdrop-filter` 미지원 폴백(불투명 배경) 필수.
- **웹폰트 절대 금지**(29항, 2회 실패 전례). 타이포 개성은 스케일·굵기로 낸다.
- **UI 작업 전에 `apple-design` 스킬을 부른다**(33항) — 새 UI·레이아웃·애니메이션·
  제스처·글라스·폰트 조정. 31항은 방향이고 이 스킬은 그 방향의 구체적 기준이다.
- **신규 페이지·대개편 전에 `docs/frontend-design-플레이북.md` 를 읽는다**(50항).
  주제 확정 → 미적 방향 1개 선언 → 토큰 4종 → 자기비평 → 코드 → 스크린샷 재비평.
  **코드부터 시작하지 않는다.**
- **전면 재디자인을 일괄 단행하지 않는다.** 새 페이지·개편하는 페이지부터 점진 적용.

---

## 7. 문체 — 화면에 나가는 모든 글

### 분석글은 명사형 개조식 (34항)

- 종결은 명사형: `~구간` `~확인` `~진단` `~수준` `~우위` `~미충족` `~진행 중`
- 금지: `~습니다/~입니다`, `~했다/~한다/~이다`, `~하세요` 권유형
- **짧게 자르라는 뜻이 아니다.** 수치·조건·해석을 다 담고 말끝만 명사로 맺는다
- 구조는 소제목 + 닷블릿. 평문 한 문단으로 흘리지 않는다
- 영문(`en/`, `...En`)은 제외 — 명사형은 한국어 장치다

### 한 문장에 주장 하나 (65항)

복문을 쓰지 않는다. 순서는 **원리 → 사실(숫자) → 판정**, 각각 한 문장씩.
판정 기준은 하나다 — **한눈에 읽히면 통과, 두 번 읽게 하면 실패.**

### 문장 부호 (80항)

- 항목과 설명, 부연, 한 줄 안의 다른 문장을 잇는 자리 → **콜론 `:`**
- 줄머리 목록 → 하이픈 `- `
- 음수·범위·연산·날짜 → 하이픈 (`-0.5%`, `RSI 40-60`, `2026-08-27`, `3-3-4`)
- **긴 대시는 어디에도 쓰지 않는다.** 폭이 넓어 시선을 끊고 복붙 시 깨진다

### 행동 촉구가 아니라 진단 (UI 텍스트 말투)

ezlong.com 은 분석·진단 리포트다. `지금 1차 매수 시작하세요` → `1차 진입 가능 구간`.
허용 패턴: `~가능 구간` `~권고` `~검토 가능` `~자제` `~대기 구간`.
**경고 트리거 문구는 강도를 유지해도 된다** — "아래 중 하나라도 발생하면 물타기 즉시 중단".

### 날짜와 시각

- **시장 문맥에 상대어 금지**(89항). `오늘·어제·내일·전일` 을 시장 날짜에 쓰지 않는다.
  절대 날짜로 적고 어느 시장인지 밝힌다 — `9월 4일(금, 뉴욕)`.
  판정·행동에는 `지금 · 이번 거래일 · 다음 장` 을 쓴다. 독자의 달력(페이지 제목)은 KST 그대로.
- **주말을 '휴장'이라고 부르지 않는다**(58항). 토·일에 미국장이 안 열리는 건 독자가 이미
  안다. `직전 장(금요일) 마감 기준` 으로 쓴다. **평일 공휴일 휴장은 예외로 살린다.**
- **거래일 판정은 언제나 캘린더가 한다**(88항). "이 데이터는 거래일에만 쌓이니 날짜가 곧
  거래일"이라는 가정은 거짓이었다.

### 화면은 독자의 것이다 (41항)

**자책도 자랑도 쓰지 않는다.** "보수적이었음을 인정한다", "예측대로" 둘 다 금지.
숫자는 그대로 공개하되, 그 뒤 문장은 **지금 시장 상태와 다음 조건**으로 쓴다.

### 답변 형식 (성동님 선호)

한국어. 서두와 말미에 KST 시각(`2026-09-12(토) 16:39`) — 샌드박스에서 `TZ=Asia/Seoul date`
한 줄로 잰다(컨테이너 시계는 틀린 게 아니라 UTC 다). 이모지·불릿 이미지 금지.
**요약할 때 표 금지**(복사해 붙이면 깨진다) — 닷블릿으로 쓴다.
배포·명령 안내는 만연체 금지, 불릿+코드블록으로 분리.

---

## 8. 프라이버시 — 개인 지칭 금지 (37항)

**사이트에 나가는 어떤 문자열에도 오너 개인을 지칭하는 표현을 쓰지 않는다.**
① 이름·호칭 ② 지시 내용의 직접 언급("~하라고 하셨으므로") ③ 개인 견해·사생활.

**`.html`·`.js`·`.css` 는 주석도 View Source 로 읽힌다.** 설계 근거는 남기되 출처를
사람이 아니라 **증상**으로 쓴다 — "운영 피드백", "이슈 제보", 또는 그냥 "증상: …".
예외는 하나뿐 — **`김성동` 저자 표기**(meta author, JSON-LD, 출간 도서 인용).

가드: `python3 scripts/check-privacy.py` 가 배포 앞단에서 막는다. 새 문구를 만들면
커밋 전에 로컬에서 한 번 돌린다.

---

## 9. 협업 경계 — 상대 저장소를 건드리지 않는다 (38항)

ezlong.com(웹)과 LongTime EasyLife(앱)는 대화방과 저장소를 따로 쓴다.
**읽기는 자유롭게, 쓰기는 하지 않는다.** 필요한 파일은 복사해 온다.

- `time/` 은 앱 세션 관할이다. 웹 세션은 손대지 않는다
- `skybluenote/` 는 별개 프로젝트다 (96항에서 오너 지시로 메타 4줄만 예외 처리)
- `scripts/brand-spiral.py` 가 나선 심볼의 유일한 원본. 앱이 확정하고 웹이 사본으로 굽는다
- `icons/` 는 앱 저장소에서 그대로 가져온다. 다시 굽지 않는다
- 브랜드 색은 배경별로 다르다 — 도메인 글자 라이트 `#2563EB` / 다크 `#3B82F6`,
  나선 심볼 라이트 `#2563EB` / 다크 `#4FC3F7`

**남겨 둘 때는 "지금은 그대로여도 괜찮다"가 아니라 "내가 나중에 무엇을 바꾸면 이 자리가
어긋나나"를 같이 적는다**(93항에서 안 적어 하루 만에 어긋났다).

---

## 10. 캐시 (32·39항)

- 캐시 정책의 단일 출처는 `firebase.json` 이다. Cloudflare 는 "기존 헤더 준수"로 고정.
  JS/CSS 는 `max-age=600` — **10분이면 자연 갱신된다.**
- **버전 쿼리(`?v=…`)는 배포가 끝난 뒤에 처음 요청한다.** 배포 중에 두드리면 엣지에
  "이 URL = 옛 파일"이 심겨 한 시간을 잃는다(2026-08-06 실제 사고).
  배포 중 확인은 원본(`ezlong-541a8.web.app`)이나 쿼리 없는 URL 로 한다.
- 10분을 넘겨도 옛 파일이면 캐시가 아니라 다른 원인(하드코딩 사본 등)을 의심한다.

---

## 11. 대량 변경의 검증 (44항)

10개 이상 파일을 한 번에 치환할 때:

1. `stable-` 태그를 **먼저** 찍는다
2. 추가·삭제 줄 수가 **정확히 대칭**인지 센다
3. 바뀐 줄 중 그 토큰이 **없는 줄이 0**인지 센다
4. **새 산출물이 실제로 커밋되는지 원격에서 확인한다** —
   `git ls-tree -r --name-only origin/main | grep <파일>`. 로컬에 있는 것과 저장소에
   실린 것은 다른 얘기다

이 조합이면 사후 원복이 한 줄이라 사전 승인이 필요 없다(30항).

**일괄 치환을 되돌릴 때는 되돌리기가 아니라 재작성으로 한다**(80항) — 1차 결과 위에
2차 치환을 얹으면 원래부터 그랬던 것과 구분할 수 없다. 원본에서 다시 만든다.

---

## 12. 판단의 기반 — 성동님 스윙 철학 (36항)

**판단 로직을 건드리기 전에 36항 전문을 읽는다**(`docs/INCIDENTS.md`).
철학은 이미 코드와 사이트 안에 적혀 있다. 새로 만들 게 아니라 찾아서 따라야 하는 것이다.

- **매수 타이밍 = 사람들이 가장 두려워할 때.** S-CORE 는 TREND 20% + MOMENTUM 30% +
  **FEAR 50%** — 심리 지표가 보스다
- **지표는 레벨이 아니라 방향으로 읽는다.** RSI 35→55(과매도 탈출)는 매수, 70→55(과매수
  냉각)는 매도
- **Gear 변속 매매법** — Gear 3(200일선 위) + RSI 40~60 이면 분할 매수 타이밍.
  **RSI 상한은 70 으로 통일**(54항). 최적대(40~60) 밖은 금지가 아니라 **비중 절반**
- **3-3-4 분할.** 1차 30% / 2차 30% / 3차 40%. 한 번에 다 사지 않는다
- **손절은 기계적으로, 감정 배제.** 익절은 RSI 75~80 에서 분할 시작
- **틀려도 좋다** — "매수했다가 안되면 매도하면 된다". 판단 보류가 안전이 아니다

**통계는 철학의 근거이지 대체가 아니다.** 우위가 없으면 *사지 말라*가 아니라 *크게 걸지
말라*는 뜻으로 읽는다 — **비중으로 답한다.**

**판정은 우리가 한다**(54항). 조건 목록을 독자에게 넘기지 않는다. 오늘 충족인지
**단정하고**, 실측 수치를 붙이고, 미충족이면 걸린 항목을 이름으로 말한다.

---

## 13. 화면이 말하는 숫자는 하나여야 한다 (88·90항)

- **단일 진실값.** 매수 점수·매도압력·Gear·보유 일수·기준일은 한 번 계산해 모든 카드가
  같은 객체를 읽는다. 같은 이름의 값이 화면에서 다르면 그 자체가 결함이다
- **상위 판정이 하위 카드를 지배한다.** 최상단이 관망이면 하위 카드의 '진입 가능'은
  코드 레벨에서 '대기'로 강제한다
- **판정은 코드가 하고 모델은 문장만 쓴다.** 모델에는 팩트시트 하나만 주고, 거기 없는
  판단·숫자는 금지한다
- **출력 검문은 모든 출구에 선다**(90항). 새 카드·새 렌더 함수를 만들면 그 자리에서
  검문을 통과시킨다. "이건 모델이 안 틀릴 것"은 근거가 아니다.
  한 절의 불릿이 통째로 걸리면 **제목까지 싣지 않는다.** 카드는 비지 않는다 —
  다 걸리면 코드가 쓴 숫자 문장으로 대체한다
- **판단 원장을 제거하거나 우회하지 않는다**(20항). AI 판단 파이프라인은 최근 3영업일
  판단 흐름을 프롬프트에 주입하고 생성 후 되기록한다. 매일 스냅샷만 보고 쓰는 "기억상실
  판단"을 막는 구조다. **새 AI 판단 기능을 만들 때도 원장 연동을 기본 포함한다**
- **근거에 이름을 붙인다**(55항). 점수를 실은 재료는 지표 이름과 수치를 적는다 —
  "경고 신호"가 아니라 "실러 CAPE 41배(장기 평균 17.8배)". 사람 말을 근거로 들면 그
  사람이나 기관 이름을 적는다. "일부 전문가"·"일각에서"는 근거가 아니다
- **통계 문장은 기저율과 같이 쓴다**(91항). "통과 시 승률 63%"가 아니라 "기저율 62% 대비
  통과 63%". 표본·기간·대상·"과거 실측이며 앞으로를 보장하지 않는다"를 반드시 붙인다

---

## 14. 공유 함수는 여러 파일에 산다 (8항)

핵심 함수가 두 곳 이상에 동시에 존재한다. **하나만 고치면 화면에 반영되지 않는다.**

- `calc_buy_score`/`calc_sell_score` — `scripts/fetch-market-data.py` **와**
  `atmr-dashboard.html`(클라이언트 재계산). `atmr-dashboard.html` 이 JSON 값을
  **덮어쓴다** — 이것이 핵심 구조다
- 수정 전 grep 으로 전체를 확인한다:
  `grep -rn "def calc_buy_score\|function calcBuyScore" . --include="*.html" --include="*.py"`
- 같은 원칙이 `bridgeOK`(4곳), 레버리지 임계값(7곳), 스파클라인 함수(2곳)에도 적용된다

**화면 문구를 만질 때도 같다** — 이 사이트에서 '앱으로 가는 길'은 세 자리다(메인 카드 ·
메인 자체 푸터 · 공용 푸터). `index.html` 은 공용 푸터를 쓰지 않는다.

---

## 15. 글로벌 헤더·푸터

모든 서비스 페이지:

- `<body>` 직후: `<script src="/ez-nav.js"></script>`
- `</body>` 직전: `<script src="/ez-footer.js"></script>`
- `<head>` 안: `<link rel="stylesheet" href="ez-design.css">`

**수정 시 개별 HTML 을 건드리지 않는다.** `ez-nav.js` 또는 `ez-footer.js` 하나만 고친다.
**생성되는 페이지의 메타는 빌더에 넣는다**(71항) — 손으로 고친 것은 다음 생성 때 사라진다.

---

## 16. 우선순위와 모델 (48항, 성동님 확정)

**① 최고의 기능 ② 효율과 자동화 ③ 안전한 개발.**
성동님은 기획자다 — 개발 이슈의 검증은 전적으로 Claude 의 몫이다. Claude 가 스스로
판단해 진행하되 커밋·태그로 원복 지점을 남긴다. 성동님은 써보고 "원복해"로 품질을 관리한다.

**모델 정책:** 낮은 모델로 여러 번 시행착오를 겪기보다 **최고 수준 모델로 한 번에
끝낸다.** 토큰을 헛되이 쓰지 않되 절약이 최우선이 아니다.

**브리핑·보고서는 HTML 이 기본이다.** 잘 디자인된 HTML 문서(아티팩트)로 전달하고,
채팅에는 복붙 가능한 닷블릿 요약을 병행한다. 채팅 텍스트에서 표는 금지, HTML 안에서는 자유.

---

## 17. 세션 마무리 — CHANGELOG

성동님 메시지에 `마무리` `정리해줘` `작업 정리` `changelog` `오늘 정리` 가 있으면
`CHANGELOG.md` 를 자동 갱신한다.

1. `git log --since="YYYY-MM-DD 00:00" --oneline --no-merges`
2. chore(봇 데이터 커밋)·keep-alive 제외하고 실제 작업 커밋만 분류
3. 파일 맨 위 `---` 다음에 새 날짜 섹션을 삽입
4. 알고리즘 변경이 있으면 **복구 git 명령어를 반드시 포함**

---

## 18. 배포 트리거 단어

성동님 메시지에 `배포` `올려` `올려줘` `push` `firebase` `deploy` 중 하나라도 있으면,
명령을 주기 **전에** 체크리스트를 먼저 실행한다.

```
[ ] 백업 스크립트 실행 완료 확인 - 미확인이면 성동님께 먼저 요청
[ ] 공유 함수 전체 grep - 동기화 누락 없는지 (14절)
[ ] .firebaseignore - 드래프트·scripts/ 제외 확인
[ ] git status - 의도치 않은 파일 포함 여부
[ ] git pull 먼저
[ ] git add [파일 명시] - git add -A 금지
[ ] commit -> push
[ ] 라이브에서 직접 기능 확인 (1절 진단 트리)
```

---
## 19. 전체 항 색인 — 94개 항이 어디에 있나

> `R` = `docs/RULES-REF.md`(기능별 참조) · `I` = `docs/INCIDENTS.md`(사고 기록).
> 상시 규칙의 **원문 서술**도 `I` 에 있다 — 위 1~18절은 그 원문에서 원칙만 추린 것이다.
> **번호는 절대 바꾸지 않는다.**

- **·** `I` 절대 규칙 — 사고방지 프로토콜 · 131줄
- **·** `I` 라이트모드 가독성 — 절대 금지 목록 · 33줄
- **·** `I` 글로벌 헤더·푸터 — 핵심 규칙 · 10줄
- **·** `R` Gemini API 모델 설정 · 26줄
- **·** `I` 세션 마무리 자동 트리거 — CHANGELOG.md 작성 · 31줄
- **·** `I` UI 텍스트 말투 원칙 — 분석/진단형 리포트 · 40줄
- **12** `R` data/ 폴더 파일 관리 규칙 — 중복 폭발 방지 · 30줄
- **13** `R` TradingView interval 충돌 금지 — 반복 사고 방지 · 50줄
- **14** `I` 폰트 14px 미만 절대 금지 — 재발 방지 · 50줄
- **15** `I` --ez-mono 폰트 가격·수치 표시 사용 금지 · 40줄
- **16** `R` GitHub Actions git 커밋 패턴 — 확정 · 42줄
- **17** `R` Massive API 보안 규칙 및 이중 JSON 아키텍처 · 51줄
- **18** `I` 작업 방식 — 위험 등급제 + 검증 분리 + 재시도 상한선 · 54줄
- **19** `R` 실시간 주가 파이프라인 이중 장애 — 재발방지 · 37줄
- **20** `I` AI 판단 3영업일 연속성 — 판단 원장(judgment ledger) 절대 보호 · 57줄
- **21** `R` 심플 주가 "장마감~일봉갱신" 공백 구간 브릿지 · 48줄
- **22** `R` NDX(나스닥) 카드 — QQQ는 나스닥 종합지수가 아니다 · 74줄
- **22-B** `R` 스파클라인 세로 여백 14% — min-max 오토스케일 착시 완화 · 20줄
- **23** `R` 스파클라인 — 프리마켓 제외, 정규장 시작(09:30 ET)부터만 표시 · 23줄
- **24** `R` bridgeOK "extSession==='post' 단독 조건"의 장마감 직후 공백 — 시간기준 보강 · 40줄
- **25** `R` TradingView 상세차트 — Baseline→Area 단색 전환 · 78줄
- **26** `R` iCloud 동기화로 인한 git index.lock 반복 재발 — Wi-Fi 일시 차단 우회 · 49줄
- **27** `R` time/data/background-manifest.json 전용 git 병합 드라이버 · 12줄
- **28** `I` 클라우드 세션 워크플로우 — 기본 작업 무대 · 20줄
- **29** `R` 이사 패키지 핵심 교훈 통합 · 49줄
- **30** `I` 중요 장면별 체크포인트·말로 하는 롤백 · 40줄
- **31** `I` 디자인 철학 — Apple + visionOS 글라스 + 테슬라/머스크 제1원리 · 31줄
- **32** `I` Cloudflare Browser Cache TTL — "기존 헤더 준수"로 확정 · 28줄
- **33** `I` apple-design 스킬 — UI 작업 시 상시 참조 · 36줄
- **34** `I` 분석글 문체 — 명사형 개조식 · 41줄
- **35** `R` 안드로이드 에뮬레이터 자동 조작 — 스크린샷·디버깅 레시피 · 64줄
- **36** `I` 성동님 스윙 철학 — 모든 판단의 기반 · 54줄
- **37** `I` 프라이버시 — 개인 지칭 금지 · 36줄
- **38** `I` 두 세션 협업 · 브랜드 원본 · 43줄
- **39** `I` 캐시 — 검증이 캐시를 오염시킨다 · 27줄
- **40** `R` 파이프라인 운영 — 돌고 있다고 믿지 말 것 · 50줄
- **41** `R` 화면은 독자의 것이다 — 자기 평가·중복 배치 금지 · 65줄
- **42** `R` 포트폴리오 점검 코너 · 77줄
- **43** `R` 이슈(A Brief History) — 손으로 쓴 것과 기계가 얹은 것 · 153줄
- **44** `I` 오늘 배운 것 — 코너 두 개를 올리고 남은 규칙 · 51줄
- **45** `I` 확인이 필요할 때의 형식 — 숫자만 답하면 되게 · 38줄
- **46** `R` 주간 노하우 수집이 이 저장소에도 닿게 · 13줄
- **47** `R` 조용히 망가지는 세 가지 · 55줄
- **48** `I` 성동님 확정 우선순위·모델·브리핑 방침 · 5줄
- **49** `R` 주간 위험 진단 코너 · 지표를 화면에 옮길 때의 네 가지 · 56줄
- **50** `I` 신규 페이지 디자인 절차 — frontend-design 플레이북 · 21줄
- **51** `I` 스코어카드 극단 판정 가드레일 · 31줄
- **52** `I` 검사에 집행권을 준다 — 스코어카드 방향·국면·크기 · 67줄
- **53** `R` 한 화면의 두 판단은 주어를 밝힌다 — 1배수 칸 vs 레버리지 칸 · 52줄
- **54** `R` 판정은 우리가 한다 — 조건 목록을 독자에게 넘기지 않는다 · 41줄
- **55** `I` 근거에 이름을 붙인다 — 익명 신호·익명 전문가 금지 · 32줄
- **56** `R` 가로로 흔들리는 페이지 — 문서 가로 스크롤을 만들지 않는다 · 30줄
- **57** `R` TimesFM 예측 범위 — 용어·적용 경계·실험실 · 34줄
- **58** `I` 주말을 '휴장'이라고 부르지 않는다 · 38줄
- **59** `R` 광고 게이트 — 앱 화면·유료 이용자에게는 광고를 부르지 않는다 · 53줄
- **60** `I` 방향 없는 재료는 채움재다 — 관망·이름 배신·금리 용어 · 66줄
- **61** `I` 여러 장을 나란히 놓아야 보이는 병 — 카드 교차 감사 · 50줄
- **62** `I` 정책금리 기대와 장기 시장금리가 따로 놀 때 · 47줄
- **63** `I` 결과가 아니라 원인을 — 코너의 정의를 코드에 못박다 · 36줄
- **64** `I` 빼기보다 혼조 — 분석이 어려운 큰 축은 카드에서 지우지 않는다 · 30줄
- **65** `I` 단문으로 써라 — 한 문장에 주장 하나 · 22줄
- **66** `I` 심층 보고서 — "아… 이런 이야기였구나" · 33줄
- **67** `I` 혼조 칸도 시장 재료만 — 개별 기업 노이즈의 뒷문을 닫다 · 17줄
- **68** `I` 보고서는 카드의 해설이지 두 번째 판정이 아니다 · 36줄
- **69** `I` watch는 달력을 보고 쓴다 — 먼 일정을 임박 재료로 쓰지 않는다 · 31줄
- **70** `I` 예정 이벤트에는 뉴욕(ET) 날짜를 적는다 · 51줄
- **72** `I` 아직 안 일어난 일은 방향이 없다 · 35줄
- **71** `R` 앱 설치 배너 — 앱 안에서는 절대 뜨지 않는다 · 44줄
- **73** `R` 애드센스 '가치 없는 콘텐츠' — 1단계: 신뢰 페이지와 청소 · 84줄
- **74** `I` 연준을 말하려면 연준이 보는 숫자를 들고 있어야 한다 · 36줄
- **75** `I` 멈췄으면 멈췄다고 말한다 — 갱신 보류 표시 · 47줄
- **76** `I` 나쁜 일에 '기대'라고 쓰지 않는다 · 68줄
- **77** `I` 블릿의 몸통도 카드의 해설이어야 한다 · 75줄
- **78** `I` 방향이 같으면 '~했으나'로 잇지 않는다 · 36줄
- **79** `R` 스마트 앱 배너는 사파리의 기능이다 — iOS 비사파리 자체 배너 · 34줄
- **80** `I` 문장 부호: 긴 대시 금지, 구분자는 콜론 · 50줄
- **81** `R` TOP9: 종목과 종목 사이를 눈으로 끊어 준다 · 38줄
- **82** `R` 내가 만든 앱은 내 사이트에서 먼저 보여야 한다 · 51줄
- **83** `I` 안전자산과 금리 수준 — 방향은 크기가 아니라 자리에서 나온다 · 93줄
- **84** `I` 혼조는 쓰레기통이 아니다 · 88줄
- **85** `I` 주가가 오른 것은 재료가 아니다 · 69줄
- **86** `I` 카드 머리와 점수 칸이 다른 이야기를 하면 안 된다 · 83줄
- **87** `R` 민감파일 차단 규칙은 저장소에 둔다 · 9줄
- **87-2** `R` 백업·원복·승계 - 맥이 사라져도, 내가 사고를 쳐도 · 26줄
- **88** `R` 거래일 캘린더 · 단일 진실값 · 출력 검문 · 109줄
- **89** `I` 시장 날짜에 상대어를 쓰지 않는다 · 123줄
- **90** `R` 검문은 '있다'가 아니라 '모든 출구에 서 있다'여야 한다 · 117줄
- **91** `R` 백테스트가 원칙 7의 전제를 무너뜨렸다 — 기각하고, 원칙 8을 붙인다 · 100줄
- **92** `R` 광고 자리에 내 앱을 건다 · 135줄
- **93** `R` OG 카드 — 판정은 절반 크기로 한다 · 128줄
- **94** `I` 선 실행, 후 확인 — 확인이 안 오면 내가 챙긴다 · 68줄
- **95** `R` 한 화면에서 같은 앱을 두 번 권하지 않는다 · 50줄
- **96** `R` 보이는 것은 누르면 간다 · 58줄
- **97** `I` AI 셋이 이어 달린다 — 인수인계서는 선택이 아니다 · 74줄

---

전체 규칙·CSS 변수·배포 체크리스트·Git 워크플로우 → **EZLONG_GUIDE.md** 참조
